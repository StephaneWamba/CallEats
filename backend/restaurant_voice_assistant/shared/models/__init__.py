"""Shared models package.

This package contains all Pydantic models used across the application
for request/response validation and serialization.

Model Modules:
    - auth: Authentication models (register, login, user)
    - calls: Call history models
    - categories: Category management models
    - delivery_zones: Delivery zone models (with GeoJSON support)
    - embeddings: Embedding generation and cache models
    - menu_items: Menu item models
    - menu_item_modifiers: Menu item-modifier linking models
    - modifiers: Modifier models
    - operating_hours: Operating hours models
    - restaurants: Restaurant models
"""
# Empty __init__.py - models are imported directly from submodules

